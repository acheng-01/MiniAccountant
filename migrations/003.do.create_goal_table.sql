CREATE TABLE goal (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    goal_month VARCHAR(10) NOT NULL,
    goal_year SMALLINT NOT NULL,
    goal_amount DECIMAL NOT NULL,
    created_by INTEGER
        REFERENCES users(id) ON DELETE CASCADE NOT NULL
);